import 'package:flutter/material.dart';

class TasksFragment extends StatelessWidget {
  const TasksFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is the Tasks Fragment"),
    );
  }
}

