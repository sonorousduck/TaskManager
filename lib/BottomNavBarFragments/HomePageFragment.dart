import 'package:flutter/material.dart';


class HomePageFragment extends StatelessWidget {
  const HomePageFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is the Homepage Fragment"),
    );
  }
}
