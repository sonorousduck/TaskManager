import 'package:flutter/material.dart';


class ScheduleFragment extends StatelessWidget {
  const ScheduleFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is the Schedule Fragment"),
    );
  }
}
